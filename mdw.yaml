endpoints:
  discovery:
    - https://mdw.useast.appfog.ctl.io/mdw
    - http://lxdenvmtc143.dev.qintra.com:7021/Discovery
  releases:
    - http://repo.maven.apache.org/maven2
  
# optional section for package groups
  packageGroups:
    base:
      routes:
        - /
      packages:
        - com.centurylink.mdw.base
        - com.centurylink.mdw.camel
        - com.centurylink.mdw.db
        - com.centurylink.mdw.hub
    demo-workflow:
      routes:
        - /services/intro
      packages: 
        - com.centurylink.mdw.demo.cool
        - com.centurylink.mdw.demo.intro
    demo-services:
      routes:
        - /services/bugs
      packages:
        - /bugs
      
# optional section for remote projects (replaces plugin.xml)
  mdw.version: 6.0.04-SNAPSHOT
  server:
    host: localhost
    port: 8080
    context: mdw
    
   
